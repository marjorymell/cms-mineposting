<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Page</title>
    <!-- CSS Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        .gradient-custom {
            background: linear-gradient(135deg, #4A00E0, #8E2DE2); /* Dark purple to light blue gradient */
        }
    </style>
</head>
<body>

<section class="vh-100 gradient-custom">
    <div class="container py-5 h-100">
        <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="col-12 col-md-8 col-lg-6 col-xl-5">
                <div class="card bg-dark text-white" style="border-radius: 1rem;">
                    <div class="card-body p-5 text-center">
                        <div class="mb-md-5 mt-md-4 pb-5">
                            <h2 class="fw-bold mb-2 text-uppercase">Edit Page</h2>
                            <p class="text-white-50 mb-5">Please update the content of the page!</p>
                            
                            <!-- Shows error message, if there are errors -->
                            {{#errors}}
                                <div class="alert alert-danger" role="alert">
                                    {{msg}}
                                </div>
                            {{/errors}}

                            <form action="/admin/posts/edit/{{page.id}}" method="post">
                                <div class="form-outline form-white mb-4">
                                    <textarea name="content" class="form-control form-control-lg" rows="5">{{page.content}}</textarea>
                                    <label class="form-label" for="typeContentX">Content</label>
                                    <!-- Shows error messages for content, if there are errors -->
                                    {{#errors.content}}
                                        <div class="alert alert-danger" role="alert">
                                            {{msg}}
                                        </div>
                                    {{/errors.content}}
                                </div>
                                <button class="btn btn-outline-light btn-lg px-5" type="submit">Update Page</button>
                                <div style="margin-top: 20px;"></div>
                                <a href="/admin" class="text-decoration-none text-white">Back to Homepage</a>
                            </form>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

</body>
</html>
