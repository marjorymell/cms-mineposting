<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Page</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        .gradient-custom {
            background: linear-gradient(135deg, #00F082, #00B9F0); /* Degradê de laranja escuro para amarelo */
        }
    </style>
</head>
<body>

<section class="vh-100 gradient-custom">
    <div class="container py-5 h-100">
        <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="col-12 col-md-8 col-lg-6 col-xl-5">
                <div class="card bg-dark text-white" style="border-radius: 1rem;">
                    <div class="card-body p-5 text-center">
                        <div class="mb-md-5 mt-md-4 pb-5">
                            <h2 class="fw-bold mb-2 text-uppercase">Create Page</h2>
                            <p class="text-white-50 mb-5">Please enter an ID and the content for the page!</p>
                            {{#errors}}
                            <div class="alert alert-danger" role="alert">
                                {{msg}}
                            </div>
                            {{/errors}}

                            <form action="/create-page" method="post">
                                <div data-mdb-input-init class="form-outline form-white mb-4">
                                    <input type="text" name="id" class="form-control form-control-lg" />
                                    <label class="form-label" for="typeIdX">ID</label>
                                    {{#errors.id}}
                                    <div class="alert alert-danger" role="alert">
                                        {{msg}}
                                    </div>
                                    {{/errors.id}}
                                </div>

                                <div data-mdb-input-init class="form-outline form-white mb-4">
                                    <textarea name="content" class="form-control form-control-lg" rows="5"></textarea>
                                    <label class="form-label" for="typeContentX">Content</label>
                                    {{#errors.content}}
                                    <div class="alert alert-danger" role="alert">
                                        {{msg}}
                                    </div>
                                    {{/errors.content}}
                                </div>
                                <button data-mdb-button-init data-mdb-ripple-init class="btn btn-outline-light btn-lg px-5" type="submit">Create Page</button>
                                <div style="margin-top: 20px;"></div>
                                <a href="/admin" class="text-decoration-none text-white">Back to Homepage</a>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

</body>
</html>
